# Streamlit Community Cloud: exactly ONE requirements.txt at repo root (no second file under webmaster-pipeline-dashboard/).
# После «Processed dependencies!» до UI: SIGSEGV — нативный слой (protobuf C++, numpy/glibc, реже watcher). См. DEPLOY.md § «Oh no».
# Не пинить watchdog отдельно: uv + своя версия конфликтуют с зависимостью streamlit (дублирующие .so → segfault).
# В app.py до pandas/google: PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python, MALLOC_ARENA_MAX=2.
# Pyarrow не пиним: дашборд не читает Parquet.
# Streamlit 1.41.x — стабильнее на части инстансов Community Cloud.
streamlit==1.41.1
numpy==1.26.4
pandas==2.2.3
protobuf==4.25.3
google-api-python-client==2.154.0
google-auth==2.36.0
google-auth-httplib2==0.2.0
