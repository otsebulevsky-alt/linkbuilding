# Streamlit Community Cloud: exactly ONE requirements.txt at repo root (no second file under webmaster-pipeline-dashboard/).
# Pin stack for Linux + Python 3.12. uv can otherwise resolve a too-new pyarrow/protobuf combo → segfault when running `streamlit` right after "Processed dependencies!".
# Also keep runtime.txt in THIS SAME ROOT (not only under webmaster-pipeline-dashboard/) so Cloud/uv picks python-3.12.x — see DEPLOY.md.
streamlit==1.36.0
numpy==1.26.4
pandas==2.1.4
pyarrow==14.0.2
protobuf==4.25.3
google-api-python-client==2.154.0
google-auth==2.36.0
google-auth-httplib2==0.2.0
