# Streamlit Community Cloud: exactly ONE requirements.txt at repo root (no second file under webmaster-pipeline-dashboard/).
# После «Processed dependencies!»: SIGSEGV / free(): corrupted unsorted chunks — нативные .so (pyarrow, glibc) + порядок импорта.
# Корневой sitecustomize.py выставляет PYTHONMALLOC/protobuf/BLAS до импорта streamlit (Secrets приходят поздно для первого импорта).
# pyarrow 11 на части образов Cloud (Python 3.11.15 + uv) даёт abort; 16.x — более свежие manylinux-колёса.
# См. DEPLOY.md § «Oh no» + secrets.toml.example.
streamlit==1.38.0
pyarrow==16.1.0
pillow==10.3.0
tornado==6.3.3
watchdog==4.0.2
numpy==1.26.4
pandas==2.0.3
protobuf==4.25.3
google-api-python-client==2.154.0
google-auth==2.36.0
google-auth-httplib2==0.2.0
